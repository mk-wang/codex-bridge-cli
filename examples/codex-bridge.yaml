upstream:
  base_url: http://127.0.0.1:4000
  api_key_env: LITELLM_MASTER_KEY
  timeout: 300
  chat_endpoint: /v1/chat/completions
server:
  host: 127.0.0.1
  port: 4010
history:
  max_cached_responses: 512
# Optional: override translator reasoning capability inference for models
# served under custom aliases in LiteLLM. Remove the block to use the default
# model-name-based inference.
#reasoning:
#  supports_thinking: true
#  supports_effort: false
#  thinking_param: thinking
